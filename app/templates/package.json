{
  "name": "<%= name %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "keywords": [
    <%- keywords && keywords.map(word => `"${word}"`).join(',\n    ') %>
  ],
  "license": "ISC",
  "repository": "<%= githubUsername %>/<%= name %>",
  "author": "<%= userName %> <<%= emailAddress %>>",
  "files": [],
  "scripts": {
    "test": "standard && ava"
  },
  "devDependencies": {
    "ava": "^0.24.0",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">=6"
  }
}
