{
  "name": "<%= name %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "keywords": [
    <%- keywords && keywords.map(word => `"${word}"`).join(',\n    ') %>
  ],
  "license": "ISC",
  "repository": "<%= githubUsername %>/<%= name %>",
  "author": "<%= userName %> <jakob@krigovsky.com>",
  "files": [],
  "scripts": {
    "test": "standard && ava"
  },
  "devDependencies": {
    "ava": "^0.21.0",
    "standard": "^10.0.2"
  },
  "engines": {
    "node": ">=4"
  }
}
